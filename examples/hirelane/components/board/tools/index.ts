/**
 * The Board's ingest layer. Mounted by `Board.tsx`, renders nothing.
 *
 * The capability list it registers is `BOARD_CAPABILITIES` in `lib/manifest.ts`, which is also
 * what the foot prints and what `test/tools.test.ts` asserts the class of.
 */
export { BoardTools } from "./BoardTools";
